include 'service-a', 'service-b', 'service-c', 'service-gateway-zuul', 'consul-demo-producer', 'consul-demo-consumer',
        'config-server', 'config-client', 'config-file'
rootProject.name = 'spring-cloud-study'